Online Bookstore Backend

This is the backend API for the online bookstore built with FastAPI and SQLite. It provides endpoints to get, add, and delete books.

Requirements

Python 3.11
Pipenv

Setup

Install dependencies

pipenv install


Activate the virtual environment

pipenv shell


Run the API

uvicorn app:app --reload
